const config = {
  baseUrlApi: 'https://hackathon2023-itmo.onrender.com/api/'

  // imageURL: (url?: string) => (url ? '' + url : ''),
  // percentVAT: 10,
  // commentSymbol: '--rating-'
  // maxSizeUploadAvatar: 1048576
}

export default config
